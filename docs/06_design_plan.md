# 06. 設計方針と実験計画(2026-08-18)

P0 完了後、P1/P2 の部品(3D 音響・経路データ・分岐提案・曲がり角誘導・予算発火)が
入った時点での棚卸し。docs/01 のロードマップと実測で出た課題を合流させ、
**実装の切れ目と実験(散歩)の回数**まで落とす。判断基準は docs/01 の一文:

> **音の鳴る方に歩くと面白い散歩ができる。**

## 課題の全量

出所: **A** = 2026-08-18 夜の実験(docs/05)、**B** = それ以前の実測、
**C** = 既存ロードマップ(docs/01)、**D** = 今回の棚卸しで立てた課題
(まだ実測に出ていないが構造上必要になるもの)。

### 柱に載せるもの(今回の設計対象)

| # | 課題 | 出所 | 柱 |
|---|---|---|---|
| 1 | 誘導の距離表現: 間隔の変化では伝わらない。間隔固定+音量へ | A | 音 |
| 2 | 誘導のピークが角の奥。手前に来て、曲がり終わりが分かる終端表現が要る | A | 音 |
| 3 | 曲がり終えると音が逆側へ消える(通過後も角へブレンドし続ける欠陥) | A | 音 |
| 4 | 誘導とビーコンの同時発火の裁定が無い(帰路で誘導を鳴らすなら必須) | D | 音 |
| 5 | 誘導・ビーコンとも頭の向きに連動していない | A/C | 頭 |
| 6 | yaw の符号が反転(今回のログで判定)。判定材料を曲がり角に絞る強化も要る | A | 頭 |
| 7 | 帰宅推定が直線×迂回率で、迂回率は経路により 1.09〜1.70 と暴れる | B | 帰路 |
| 8 | 帰路ビーコンは直線方向を指す(川・私有地越しにも鳴りうる) | D | 帰路 |
| 9 | 予算係数(速度)が固定。ランニング時の実測は 94 m/min | B | 帰路 |
| 10 | 交差点ごとの局所評価では、曲がった直後が良くてもその先がつまらない | A | 行き先 |

### 後段に送るもの(この束では手を付けない)

| 課題 | 出所 | 扱い |
|---|---|---|
| ビーコンの歩調同期・距離キュー(音量+ローパス+リバーブ) | C | 柱「音」の距離→音写像を共有する前提で後段。誘導の音量符号化が先例になる |
| earcon 音源のデザイン差し替え | C | 音の構造(何を何で符号化するか)が固まってから |
| 上下方向(坂・階段・歩道橋) | B | 将来。なお「方向が実質 3 択」は、分岐提案+誘導が実在の道の方位を連続値で指すようになった時点で構造的に解消済み |
| way 単位の馴染み度(セル → way) | C | 行き先ゾーンの効果を見てから。ゾーン集計はセルのままで成立する |
| 古い通過記録を無視する窓 | A(案) | 半減期 45 日が既にあり、行き先ゾーンで挙動が大きく変わるため、その実測後に判断 |
| 通勤路なぞり UI・オンボーディング | C | P2 終盤 |
| 延長了解音が suggestion 兼用 | B | 語彙 5 種の原則との兼ね合い。実害が出ていない |
| 60〜90 分の長いセッション(電池・バックグラウンド) | D | 専用実験はしない。長めの散歩をした回のログで受動観察 |
| Siri / HealthKit / ODbL 表記 / App Store 提出 | C | P3 |

### 保留(方針決定済み・変更なし → docs/05)

首振り誤検出 約 9%、応答未認識の処理、誤って始まった帰路の取り消し、
うなずき閾値 ±8° の誤検出増加傾向。いずれも「困るまで動かさない」。

### 道具の手直し(散歩不要・最初に片付ける)

| 不具合 | 実測 |
|---|---|
| summarize が「提案(分岐)」を「鳴った」に数えない | 実機で 10 件鳴ったログの要約が「鳴った=0」 |
| replay の却下内訳が旧ゲート(スコア絶対下限)の文字列のまま | 本体は比ゲートに変更済み。内訳表示だけが嘘になる |
| replay が複数散歩の混在ログで帰路をまとめて計算する | 2 回分のログで迂回率 4.63 という無意味な値が出た |

## 設計方針(4 本の柱)

### 柱 1: 音 — 距離→音の写像を 1 本にし、誘導を作り直す

**間隔で距離を表すのは失敗だった**(2026-08-18 実測・利用者判断)。間隔は固定し、
**音量で近さを表す**。

- 間隔: `guidance_interval_sec`(固定。仮 1.2 秒)。「連続音である」ことだけを間隔が担う
- 音量: 角までの距離を `guidance_gain_far` → `guidance_gain_near` へ単調に上げる。
  **ピークは角そのものではなく手前**(`guidance_peak_before_m`。仮 10 m)で最大に達し、
  以降は維持する。「あの角だ」と確定してから曲がる準備をする時間を作る
  (実測では間隔最小が角まで 5 m 前後 = 曲がっている最中に来ていた)
- 定位のブレンド(角→曲がる先)は**接近中に限る**。最接近を過ぎたら角へ引き戻さない。
  現行式は距離だけを見るため、通過後に「背後になった角」へ再ブレンドし、
  右に曲がり終えた直後に音が左へ流れる(12:39:52〜58 の実測で +56° → −33°)
- 終端: **曲がり完了**(進行方位と分岐方位の差が `branch_straight_deg` 以内に収まる)を
  検出したら、正面定位のまま音量を数音かけて減衰させて終える
  (`guidance_closing_tones`。仮 3 音)。「イベントが終わりかけている」が音で分かる。
  曲がらずに離れた場合は現行どおり打ち切り(`guidance_left_behind_m`)
- 裁定: 同時発火の優先順位を **プロンプト > 誘導 > 提案 > ビーコン** と定める。
  誘導が鳴っている間はビーコンを休止する(帰路で誘導を入れる柱 3 の前提)
- 実装位置: 距離→音量の写像は EarconSynth 側の 1 モジュールにし、
  後段のビーコン距離キュー(音量+ローパス)がそのまま共有できる形にする

リスク: 屋外の環境音の下で音量差が判別できるかは実測でしか分からない。
外れた場合の代替は音数(1 音 → 2 連音)か音高で、写像モジュールの差し替えで済ませる。

**この作り直しの後は、案内音の微調整を単独目的にした実験は組まない**
(2026-08-18 利用者判断。大局優先)。散歩のついで観察に格下げする。

### 柱 2: 頭 — 定位の基準を「進行方位」から「進行方位+首のオフセット」へ

提案・誘導・ビーコンのすべてが対象。適用点は SoundPlacement へ渡す基準方位の合成
1 箇所に集める(音の種類ごとに配線しない)。

- **符号: yaw は負号を掛けてから使う。** CoreMotion の yaw は反時計回りが正、
  方位は時計回りが正で、HeadingFusion は「揃っている」と仮定していた。
  今回のログの自動判定は 35 対中 10 一致(29%)で反転を支持。規約どおりの結論
- 判定の強化: 直進中の対(course がほぼ動かない)は符号情報を持たず判定を薄める。
  **|Δcourse| ≥ 30° の対だけ数える**ようにし、全ログ一括で再判定して確定させる
  (前回ログの 45% という宙ぶらりんな値は直進の対が混ざったもの)
- 基準線: course 有効時のみ育て、再装着でリセット(実装済み)。今回のログで終始
  「未確立」なのは flag OFF で ingest を止めている仕様どおりで、バグではない
- ゲート: 基準線未確立 / course 無効 / 再装着直後は従来どおり進行方位のみ。
  `use_head_orientation` の逃げ道フラグは残す
- ログは現行どおり「進行基準なら」を併記し、散歩後に切り分けられる形を保つ

### 柱 3: 帰路と予算 — 直線と係数をやめ、経路と実測で約束する

「約束を守る」(docs/01)の実装を、推測(直線 × detour_factor × 固定速度)から
実測(経路グラフの長さ ÷ 実測速度)へ置き換える。

- **GraphRouter**(Core・純粋): WalkGraph 上の Dijkstra。
  コスト = 線分長 × (1 + `cross_cost_weight`×横断 + `way_class_weight`×序列)。
  分岐スコアと同じ重みを再利用し、価値観を一箇所に保つ
- **帰宅推定 = 自宅までの経路長 ÷ 実測速度。** 発火条件・許容半径・
  延長の「帰宅ぶん確保」もすべて経路長ベースへ乗り換える。
  直線 × `detour_factor` は地図なし・圏外のフォールバックに格下げ
  (迂回率の実測 1.09〜1.70 は、固定係数では安全側にも危険側にも外れることを示した)
- **帰路中は経路の次の曲がりへ誘導音を張る**(散策と同じ音・同じ仕組み)。
  ビーコンは「自宅の在り処」の意味を守ったまま残し、誘導中は裁定(柱 1)で休止。
  直線ビーコンだけに頼る現行の「川・私有地越しに鳴る」問題はこれで解消する
- 逸脱: スナップ先の way が経路から外れ続けたら再計算(頻度上限を設ける)。
  帰路も「叱らない」原則のまま、経路は押し付けではなく音の案内の材料にとどめる
- **速度の実測化**: GaitMetrics のセッション内平均(ウォームアップ後)を、
  散歩間の指数移動平均(永続)とブレンドして使う。`walking_speed_m_per_min` は
  初期値に格下げ。ランニング検出のような分類は設けない(平均が追従すれば足りる)

### 柱 4: 行き先 — 「面白い」は広域で決め、局所で実行する

交差点ごとの局所評価(BranchSuggester)はそのままに、**上に 1 層足す**。

- **ゾーン集計**: `zone_size_m`(仮 300 m)四方でセル馴染み度を集計し、
  道の密度(way の総延長)で重み付けした新鮮さを出す。道が無いゾーンに価値はない
- **行き先の選定**: セッション開始時に、予算の許容半径内で最良のゾーンを
  「行き先」に選ぶ。到達か通過で次を再選定。選定理由(方角・距離・新鮮さ)をログに残す
- **targetBias**: 分岐スコアに「行き先方位との整合」の項を足す。
  homewardBias と同型・逆向きで、`target_bias_weight` を掛ける。
  帰宅バイアスが立ち上がるにつれ行き先の寄与は線形に畳む(帰宅が常に優先)
- 採らなかった案: **ループ経路の事前計画**(散歩全体をグラフで引く)。
  「提案は無視してよい」(docs/01 の 3 原則)と衝突し、無視のたびに再計画が要る。
  ゾーン+バイアスは無視に頑健で、既存スコアラーに 1 項足すだけで済む。
  行き先が固定される帰路では逆に経路計画が正解で、それは柱 3 が担う

## 実装の切れ目と実験計画

検証 3 段(ユニット → 再生 → 散歩)は docs/05 のとおり。散歩でしか確かめられないのは
**音の聞こえ方と「面白いか」だけ**で、それ以外は全て机上で潰してから歩く。

| 段 | 実装 | 机上検証(散歩不要) | 散歩で見るもの |
|---|---|---|---|
| 0 ✅ | 道具 3 点(summarize / replay) | その場で再実行 | — |
| 1 ✅ | 誘導音の作り直し(音量・固定間隔・頂点・終端・片道ブレンド・裁定) | ユニット 9 件(`TurnGuidanceTests`) | **α**: 音量で近さが分かるか、終端で曲がり終わりが分かるか |
| 2 ✅ | yaw 負号+判定強化 → 全ログで符号確定 → `use_head_orientation` ON | 再生(全ログ一括で **反転と確定**)+ ユニット 3 件 | **α**: 意図的テスト 1 個「歩きながら顔だけ左右」 |
| 3 | GraphRouter・経路長ベース推定・帰路誘導・速度の実測化 | 再生(過去の全帰路で推定誤差を現行モデルと比較。ランニング入りログで発火時刻の差) | **β**: 誘導だけで帰れるか |
| 4 | ゾーン集計・行き先選定・targetBias | 再生(過去ログで提案が未踏ゾーン方位へ寄るか、頻度が変わりすぎないか) | **β**: 音の鳴る方に歩いて面白いか |

- **散歩 α**(段 1+2 の後): 普通に歩く+意図的 1 個(顔だけ左右)。誘導音と符号の
  どちらが外れても「進行基準なら」併記ログで切り分けられる
- **散歩 β**(段 3+4 の後): 意図的 0 個。普通に歩き、帰路は誘導に従ってみる。
  推定誤差・経路逸脱・ゾーン選定は全て自動でログに残る。主観で答えるのは
  「面白かったか」だけ
- **散歩 γ**: β の実測でパラメータを振った後の確認。必要になった場合のみ

実装 4 段(+道具)に対して散歩は 2〜3 回。段 1+2 を先にするのは、
(a) 小さく早く終わる、(b) 散歩 β の「面白いか」の評価を音の欠陥で汚さないため。
段 3 を段 4 より先にするのは、GraphRouter が行き先の到達可能性判定にも使える基盤であり、
帰路は目的地が固定で評価が客観的(推定誤差)= 再生だけで大半を検証できるため。

## この束の後に残る主なもの

ビーコンの歩調同期と距離キュー拡張、earcon デザイン、way 単位馴染み度、
なぞり UI、上下方向、P3 一式(順序は docs/01)。
