# 05. フィールドテストの進め方

実機 + AirPods でしか確認できない挙動(位置精度・ヘッドフォンモーション・音の定位)を
段階を分けて確認し、閾値調整の材料をフィールドログとして残す。

## 検証の方針(2026-08-18)

**実機テストは実装ごとに行わない。** 1 要素 1 実験では回数が過剰になる。検証手段を 3 段に分け、
実機でしか確かめられないものだけを実機に回し、**まとめて 1 回で見る**。

| 手段 | 対象 | コスト |
|---|---|---|
| ユニットテスト | Core の純粋ロジックの正しさ | `scripts/test.sh` |
| **ログ再生** | 記録済みの実データに対する振る舞い。経路長・迂回率・提案の判定・パラメータの振り直し | `scripts/replay_log.sh` |
| 実機テスト | 音の聞こえ方、ジェスチャ、OS の挙動(バックグラウンド継続・装着検出)、GPS の実際の質 | 散歩 1 回 |

ログには**位置更新を 1 件ずつ `fix` 行として残している**。これがあるかぎり、純粋計算の
変更は歩き直さずに確かめられる。パラメータを振って比べることもできるので、
「値を変えて試すために歩く」は不要。

実例(2026-08-17 の迂回率の問題): 精度フィルタの効果は再生で確認できた。
フィルタなしでは経路長 649 m・迂回率 2.85 に対し速度積分は 250 m という水増しの署名が出て、
精度フィルタを入れると 228 m・1.00 で積分と一致した。区間フィルタ単独では効果がなく、
当初の見立てが誤っていたことも同じ再生で分かった。**散歩は要らなかった。**

### 実機テストの組み方

- 実装のたびに歩かず、**未検証の項目を貯めてから 1 回にまとめる**
- 観察項目を 2 種に分ける
  - **普通に歩けば自動で記録されるもの**(いくつあってもよい)
  - **意図的に試すもの**(手順を覚えて実行する必要があるので **1〜2 個まで**)
- 実機に回す前に「これは再生で確かめられないか」を必ず問う

## ログの取り出し

散歩中は画面を見られないため、判断材料は端末内の TSV(`otosanpo-field-log.tsv`)に追記している。
取り出しは**実機を操作しない経路**を既定とする。

1. iPhone のアプリ > フィールドログ > **「ログを書き出す」** > AirDrop で Mac へ
2. 受信したファイルを `field-logs/inbox/` に入れる(Finder でドラッグ)
3. `scripts/import_log.sh` を実行

`import_log.sh` は `field-logs/` の中だけで完結する。`~/Downloads` など PC の他のフォルダは
読まないし変更しない。取り込んだファイルは inbox から `field-logs/field-log-<書き出し日時>.tsv`
へ移動する(残すと次回に前回のログを読んでしまうため)。

- 取り込みと同時に `scripts/summarize_log.sh` で要約を出す。ログは帰路ビーコンだけで数百行になり、
  そのままでは判断に要る行が埋もれるため
- 要約の方針: 判断材料になる行(状態遷移・提案・ジェスチャ振幅・方向の取得元)は全件、
  量が多く 1 行ずつには意味の薄い行(ビーコン)は間引いて傾向だけ
- `scripts/summarize_log.sh <ファイル>` で取り込み済みのログを再要約できる
- USB 接続の実機から直接取り出す `--usb` も残してあるが、実機に触れるため既定では使わない
  (CLAUDE.md「実機へのインストールと起動は人が Xcode から行う」)
- テストの区切りではアプリ内の「ログを消去」を押してから歩き始める。セッションの切り分けが楽になる

## 段階

| 段階 | 確認すること | 状態 |
|---|---|---|
| 1 | 実機ビルド・署名・権限フロー | 完了(2026-08-16) |
| 2 | 画面消灯・ポケット収納でモーションと Timer が継続するか | 完了(2026-08-16)。ログ上、20 分間モーションが 50 Hz で届き続け、10 分後と延長後のタイマーが両方発火した |
| 3 | 屋外で提案音が鳴るか、進行方向が取れるか | 完了(2026-08-16。精度に課題) |
| 4 | 時間到来 → ジェスチャ応答 → 帰路ビーコンで帰宅 | 完了(2026-08-16。課題は下記) |
| 5 | 閾値調整後の通し確認 | 目的は段階 6 に統合(2026-08-17)。この回ではジェスチャ 2 点を検証できず、影検出を実装して測り直した |
| 6 | course ホールド後の帰路確認、ジェスチャの誤検出率の実測 | 完了(2026-08-17)。下記 |
| 7 | 方向パラメータ再調整後の確認 | 完了(2026-08-17)。下記。**P0(フィールドテスト)は完了** |
| 8 | 到着半径 10 m・経路長フィルタ・ビーコン繰り上げ・プロンプト保留の確認 | 完了(2026-08-17 夜)。下記 |
| 9 | 精度フィルタ後の迂回率、プロンプト保留(未検証分) | 完了(2026-08-18)。迂回率は実測どおりと判明、プロンプト保留は机上で検証 |

## 課題と対策方針の一覧(2026-08-17 時点)

実測で判明した課題と、それぞれをどう扱うと決めたか。詳細は各リンク先。

### 対策済み(実測で確認するのが残っている)

| 課題 | 対策 | 状態 |
|---|---|---|
| 軽いうなずきが検出されない | 閾値 ±10° → ±8° | 検出を確認。誤検出率 約 2% で許容 |
| コンパス退避が左右を反転させる | 退避を無効化 | 中央で鳴る割合が 92% に悪化したため course ホールドを追加 |
| ビーコンが中央でばかり鳴る | course ホールド 15 秒 + 採用条件を精度 70° まで緩和 | **段階 7 で確認** |
| 提案が同じ場所で繰り返し鳴る | 直進との差 0.05 未満は鳴らさない / 30 m 進むまで次を出さない | 実測で頻度低下を確認 |

### 方針が決まっていて未着手

| 課題 | 方針 | 時期 |
|---|---|---|
| 曲がり角の 10 m 手前で鳴る | 交差点接近の検出で提案タイミングを決める(タイマー駆動をやめる)。位置精度 3〜6 m は十分と確認済み | P2 |
| 私有地を突っ切る方向を指す | OSM way で通行可否を持つ | P2 |
| 折り返し(来た道を逆行)を指す | OSM 経路グラフで正面から解く。方位だけで抑える緩和案は**不採用** | P2 |
| 太い道路の横断コスト | OSM way のタグ(幅員・信号)から横断コスト項をスコアに入れる | P2 |
| ~~ビーコンが歩調と無関係~~ | **実装済み(2026-08-19)**。CMPedometer の歩調に同期。距離は音量へ移した(docs/03) | 済 |
| ~~顔の向きが定位に反映されない~~ | **不成立(2026-08-19)**。yaw が旋回を追えていない(docs/03) | 取り下げ |
| 方向が実質 3 択 | 12〜16 方向 + 上下へ。earcon を 5 種から増やさずに分解能を上げる方法は設計課題 | P2 |

### 保留(対処しないと決めたもの)

| 課題 | 判断 |
|---|---|
| 首振りの誤検出 約 9% | **頻発して困るまで対処しない**(2026-08-17 判断)。失うのは時間だけで取り返しがつく。影検出で率は記録され続けるので悪化に気づける |

### 方針決定済み・対処なし(2026-08-17 判断)

| 課題 | 判断 |
|---|---|
| 予算を使い切ると同じ提案を鳴らし続ける | **専用の伝え方は設けない。** 予算切れは「設定した時間を使い切った」ことであり、時間到来イベントとそれへの応答(帰る / 延長)で既に解決している。鳴る回数自体は提案の間引き(直進との差・30 m 移動)で対処済み |
| 時間到来への応答が認識されなかった場合の処理 | **保留**(首振りの誤検出と同じ扱い)。60 秒ごとに再度プロンプトが鳴るので、実害が出てから考える |
| 応答待ち中に AirPods を外した場合の扱い | **再装着まで待つ。** 現状すでに待つ実装になっており(応答待ちのまま 60 秒ごとに再プロンプト)、差分は「外している間も鳴り続ける」点だけ。外している間は鳴らさず再装着時に鳴らし直す修正を P1 で行う |
| 誤って始まった帰路を取り消せない | 「終了」ボタンのみ。首振りの誤検出を保留にしたため、これも当面そのまま |

## 2026-08-16 のテストで出た課題

段階 3・4 を 1 回の散歩で実施。時間到来は 2 回発火し(1 回目は延長)、提案音・ジェスチャ応答・
帰路ビーコンはいずれも鳴った。以下は精度・体験の課題であり、詳細と対処方針は各ドキュメントに置く。

- 提案が曲がれない場所で鳴る(10 m 手前・私有地) → docs/04「曲がれない場所で鳴る問題」
- 同じ道の折り返し指示、太い道路の横断コスト → docs/04「歩ける道とのマッチング」
- 軽いうなずきが検出されない(首振りは 1 回で検出) → docs/04「頭ジェスチャ検出」
- ビーコンが歩調と無関係、角を曲がってから方向反映まで 1〜2 音遅れる → docs/03「ビーコンの距離・方向キュー」
- 顔の向きと進行方向のずれ、方向が実質 3 択 → docs/01 ロードマップ P1/P2

### ログを読んで初めて分かったこと

体感では「方向の反映が遅れる」と見えていたものに、別の原因が混ざっていた。

- **コンパス退避が逆側の定位を出していた**。方向判定 194 回のうち 38 回が退避で、
  帰路の 2 例で左右が反転していた → docs/04。`allow_compass_fallback` を false にした
- **提案が 4 分間に 9 回、すべて「右」方向で鳴っていた**(05:42〜05:45)。この間 `bias` は
  1.00(最大の帰宅寄せ)、`許容` は 175 m → 0 m まで縮んでおり、実質「引き返せ」を
  25 秒ごとに繰り返していた。予算を使い切った状態に別の伝え方が無く、同じ提案を
  鳴らし続ける構造になっている(未対処)
- **AirPods の接続が歩行中に 6 回途切れて復帰していた**(05:46・05:50・05:51・05:52 に集中)。
  **不具合ではなく、人と会話するために外した**ことによる(本人確認済み)。装着検出は
  意図どおり動いている。ただし外している間はジェスチャを受け付けられないため、
  応答待ち中に外れた場合の扱い(再装着まで待つか、時間切れとするか)は未整理

## 段階 5 の観察項目

閾値調整後の通し確認。手順は段階 3・4 と同じ(ログ消去 → 10 分 → 開始 → 時間到来で
うなずき → ビーコンで帰宅 → ログを書き出して inbox へ)。今回の変更に対応する観察点:

1. **軽いうなずきが通るか**(`nod_pitch_threshold_deg` ±10° → ±8°)。
   通らなければ、はっきりうなずいた場合との差をログで比較する
2. **応答待ち中に、うなずいていないのに帰路が始まらないか**。始まったら ±10° に戻す。
   これが今回の変更で最も見たいこと
3. **ビーコンが中央でばかり鳴らないか**(コンパス退避の無効化)。左右が付く頻度が落ちすぎて
   案内として成立しないなら、course のホールドを実装する
4. 角を曲がった後、何音目で左右が反映されるか(退避を切った状態での純粋な遅れの量)
5. 応答待ちの最中に AirPods を外した場合どうなるか(会話などで外すのは通常の使い方。
   再装着すれば応答できるのか、60 秒の応答待ちが先に切れるのか)

## 2026-08-17(段階 5)の結果

行き(11:45〜11:51、手動終了)と帰り(16:13〜16:18、帰路イベントをデバッグボタンで発火)の
2 回に分けて実施。ログを消去せずに書き出したため前回の散歩が混在していた
(`scripts/summarize_log.sh --last` で最後の 1 回に絞れるようにした)。

### 主目的の 2 点は検証できていない

| 観察項目 | 結果 |
|---|---|
| 軽いうなずきが通るか | **未検証**。検出時の pitch 振幅は 42.6° で、従来の ±10°(振幅 20° 要求)でも通る大きさ。軽いうなずきは試されていない |
| うなずかずに帰路が始まらないか | **未検証**。応答待ちが 2 秒で終わった(すぐうなずいたため)。誤検出を観察できる長さがない |

当初は「応答待ちを 60 秒使い切って誤検出を観察する」手順を提案したが、**取り下げた**。
散歩中に「帰りますか?」と聞かれた人が長考するのは不自然で、実際の応答は数秒で終わる。
測りたいものをテストの手順で補うのは筋が悪い。

代わりに**影検出**を実装した(docs/04)。応答待ち以外でも同じ検出器を回して記録だけ
残すので、普通に散歩するだけで誤検出率が 1 回分まるごと得られる。うなずきは
**いつもどおり自然に**すればよい。

### コンパス退避の無効化は、想定より大きな副作用を出した

帰路 165 秒のあいだにビーコンは約 118 回鳴ったが、左右が付いたのは **10 回だけ**
(約 92% が中央)。進行方向の取得が 8 回途切れ、復帰しても 1〜3 秒でまた失われていた。

| 区間 | 長さ |
|---|---|
| 16:15:59〜16:16:12 | 13 秒 |
| 16:16:15〜16:16:31 | 16 秒 |
| 16:16:32〜16:16:49 | 17 秒 |
| 16:16:50〜16:17:06 | 16 秒 |
| 16:17:07〜16:17:41 | 34 秒 |
| 16:17:44〜16:18:04 | 20 秒 |
| 16:18:05〜16:18:10 | 5 秒 |
| 16:18:12〜16:18:36 | 24 秒 |

段階 5 の観察項目 3 で「左右が付く頻度が落ちすぎるなら course のホールドを実装する」と
決めていたとおり、**course ホールドを実装した**(`course_hold_sec` = 30 秒。docs/04)。
途切れの多くは 30 秒以内に収まるため、これで大半は左右が付く見込み。

**なぜ course が棄却されたかはログに残っていなかった**(棄却時は生値を記録していなかった)。
棄却理由(速度不足 / fix が古い / 精度不足)と生値を残すようにした。中央で鳴らした
ビーコンも数えられるよう記録する。

### 位置精度は測っていなかった

「どれくらいの精度で位置を観測できているか」はログから答えられなかった。
`CLLocation.horizontalAccuracy` を一度も読んでいなかったため。要求しているのは
`kCLLocationAccuracyBest` + `activityType = .fitness` だが、実際に何 m で返ってきていたかは
記録がない。間接的な材料は `courseAccuracy`(有効時 20〜40°、棄却時 52〜102°)だけで、
これは fix の質が落ちていた傍証にはなるが位置そのものの誤差ではない。

`horizontalAccuracy` をログに出し、要約で最良 / 平均 / 最悪を集計するようにした。
道路スナップ(docs/04「歩ける道とのマッチング」)が要るかどうかは、この実測を見てから判断する。

## 2026-08-17 夜(段階 6)の結果

約 7 分の散歩 1 回。ログを消去してから実施したので混在なし。

### 位置精度は十分だった

**最良 3 m / 平均 4 m / 最悪 6 m**(90 件)。道路の間隔に対して十分細かく、
**OSM way への道路スナップは位置精度の面では成立する**見込み。
「10 m 手前で曲がれと言う」「私有地を突っ切る」の原因は位置精度ではなく、
道路データを持っていないこと(および提案がタイマー駆動であること)だと確認できた。

### ジェスチャ: 問題はうなずきではなく首振りだった

影検出により、閾値の誤検出率が実測できた → docs/04。うなずきは ±8° で約 2%(許容範囲)、
**首振りは約 9%** と高い。しかも首振りは閾値を上げると意図した動作のほうが先に落ちるため、
パラメータでは解けない。判別方式の見直し(適応的閾値・周期による分離)が要る。

軽いうなずきは検出された(応答待ち中の pitch 振幅は最大 10.0°)。±8° への引き下げは
狙いどおりに働いたと見てよい。ただし ±10° なら落ちていたかどうかは、検出器の窓
(1.6 秒)の振幅を記録していないため断定できない。

### course ホールドは効いたが、古い向きを指す区間が出た

中央で鳴るビーコンは 92% → 13 回(全 86 回中)に改善。ただし内訳は
**course=7 回 / 保持=68 回**で、course が有効だったのは約 9% だけだった。
26 秒前の向きを使って右いっぱいに振った区間があり、コンパス退避で起きていた
「逆側の定位」が再発している → docs/04。`max_course_accuracy_deg` 45° → 70°、
`course_hold_sec` 30 秒 → 15 秒に調整した。

## 段階 7 の観察項目

1. ビーコンの内訳が **course 主体**に変わったか(要約の「進行方向の取得元」を見る)
2. 角を曲がった直後に逆側を指す区間が消えたか
3. 影検出の回数(首振りの誤検出率が変わらないことの確認。判別方式を変えるまでは高いまま。
   悪化していないかだけ見る)

4. **歩行の実測**(要約の「歩行の実測」)。帰宅プロンプトの発火条件を予算模型に統合する
   準備として、平均歩行速度と迂回率の実測が要る(docs/03)。段階 7 の散歩に計測を
   仕込んであるので、**別に実測用の散歩をする必要はない**。同じ 1 回で兼ねる。
   ただし迂回率は帰路でしか取れないため、**途中で終了せず自宅まで歩き切ること**

これが通れば P0(フィールドテスト)は完了。以降は docs/01 のロードマップ P1 / P2 に移る。

## 2026-08-17(段階 7)の結果 — P0 完了

約 15 分の散歩 1 回。**目的地を定めず歩いた**ため、提案に従った経路ではない。
迂回率は目的地の定まった帰路から取っているので、この点の影響は受けない。

### 方向の問題は解決した

| 指標 | 段階 5 | 段階 6 | 段階 7 |
|---|---|---|---|
| 方向の取得元 course | — | 7 回 | **165 回** |
| 同 保持 | — | 68 回 | **2 回** |
| 同 端末コンパス | 38 回 | 0 回 | 0 回 |
| 中央で鳴らした(左右なし) | 約 92% | 13 回 | **0 回** |

`max_course_accuracy_deg` を 70° にしたことで course がほぼ常時有効になり、ホールドは
本来の「短い橋渡し」に戻った(15 分で 2 回)。角を曲がった直後に逆側を指す問題も
体感で減っている。**コンパス退避 → ホールド → 採用条件の緩和、という 3 手で決着した。**

### 予算模型の仮置き係数は、実測とほぼ一致していた

| 係数 | 設定値 | 実測 |
|---|---|---|
| `walking_speed_m_per_min` | 70 | **74**(帰路)/ 72(散歩全体) |
| `detour_factor` | 1.30 | **1.32** |

帰路は直線 317 m / 経路長 419 m / 所要 5.0 分。模型の予測は 5.9 分で、実際は 5.0 分。
**安全側に外れている**(早めに帰路へ入る)ので、性質としては望ましい方向。

ただし**この迂回率 1.32 は過大**である疑いが強い。位置更新は 1 秒ごとに来るが、
歩行では 1 秒 = 約 1.2 m しか進まない一方、水平精度は 3〜5 m ある。毎回の差分を
そのまま足すと GPS の揺れが経路長として累積する。速度の積分から求めた移動距離は
約 369 m(平均 74 m/min × 5.0 分)で、経路長の 419 m とは 50 m の差がある。
真の迂回率は 1.2 前後の可能性が高い。

→ `path_segment_min_m`(10 m)を追加し、**これ未満の差分は揺れとして捨てる**ようにした。
基準点は動かさないので、真の移動は次回以降の更新で拾われる。段階 8 で効果を確認する。

### 影検出は横ばい

33 回(うなずき 9 / 首振り 24)、約 15 分。1 分あたりではうなずき 0.6 回・首振り 1.6 回で、
段階 6(0.43 / 1.9)と同程度。**閾値変更による悪化はない。** 首振りの誤検出は保留の方針どおり。

### 位置精度

最良 3 m / 平均 4 m / 最悪 5 m(168 件)。段階 6 と同じ水準で安定している。

## 段階 8 の観察項目

P0 は完了しているので、以降は P1 / P2 の実装と**並行**して回す。普通に散歩するだけで
以下が確認できる。手順は従来どおり(ログを消去 → 開始 → うなずき → 自宅まで歩き切る)。

| # | 見るもの | 期待 | 外れた場合 |
|---|---|---|---|
| 1 | **到着音が鳴るか**(`arrival_radius_m` 40 → 10 m) | 自宅前で鳴る | 鳴らずビーコンが続くなら 15〜20 m へ広げる。水平精度 3〜5 m に対し 10 m は余裕が小さい |
| 2 | **迂回率の実測**(`path_segment_min_m` = 10 m の効果) | 1.32 → 1.2 前後に下がる | 変わらなければ揺れ以外の要因。速度の積分との差を再確認する |
| 3 | **角を曲がった直後の反映**(ビーコン繰り上げ) | 曲がってすぐ 1 音目に反映される。ログに「ビーコン繰り上げ」が出る | 繰り上げが多すぎて音が increasing に感じるなら `beacon_direction_change_deg` を上げる |
| 4 | **応答待ち中に AirPods を外す**(プロンプト保留) | 外している間は鳴らず、再装着で鳴り直す | ログの「プロンプトを保留します」「鳴らし直します」で確認 |
| 5 | 方向の取得元が course 主体のままか | 段階 7 の水準(course ほぼ 100%) | — |

**4 は意図的に試す必要がある。** 時間到来の音が鳴ったら、応答せずに AirPods を片方外し、
数十秒たってから付け直す。それ以外は普通に歩けばよい。

## 2026-08-17 夜(段階 8)の結果

約 15 分の散歩 1 回。

| # | 見たもの | 結果 |
|---|---|---|
| 1 | 到着音(10 m) | **鳴った。** ビーコンの最近距離 12 m まで詰まり、到着判定も成立。10 m で問題ない |
| 2 | 迂回率 | **1.67 に悪化**(前回 1.32、期待 1.2)。原因は別にあった。下記 |
| 3 | ビーコン繰り上げ | **動作した。** 4 分の帰路で 2 回(31°・52° の変化)。鳴りすぎてはいない |
| 4 | プロンプト保留 | **未検証。** 外している間に再プロンプトの時刻(60 秒後)が来なかったため、保留の経路を通っていない |
| 5 | 方向の取得元 | course=142 / 保持=1 / コンパス=0、中央 0 回。段階 7 の水準を維持 |

### 迂回率が悪化した原因は「小さな揺れ」ではなく「大きく飛んだ fix」だった

前回入れた `path_segment_min_m`(10 m 未満を捨てる)は、**問題の逆側を塞いでいた**。

- この散歩では水平精度が **最悪 69 m** まで落ちた区間があった(前回は最悪 5 m)
- 同じ区間で `CLLocation` 自身が **3.60 m/s**(時速 13 km。歩行ではありえない)を報告していた
- 精度 69 m の fix は 10 m の下限を軽く超えるので、フィルタを素通りして経路長に乗る。
  1 件で 100 m 規模の水増しになりうる

→ **水平精度が `max_accuracy_for_metrics_m`(20 m)より悪い fix は丸ごと捨てる**ようにした。
経路長にも平均速度にも入れない。捨てた件数もログに出し、実測値の信頼度を後から判断できる
ようにした。段階 9 で再確認する。

### 再装着時に姿勢の基準が跳ぶ(新たに判明)

応答待ち中に AirPods を外して付け直した際、装着直後のサンプルに
**yaw 振幅 203.7°**(および 175.3°)が記録されていた。`CMHeadphoneMotionManager` は
接続ごとに姿勢の基準を引き直すため、yaw が不連続に跳ぶ。窓に残った古いサンプルと
繋がれば首振りに化ける。今回は再装着直後のサンプルが疎(1〜2 Hz)だったため
検出には至っていないが、**構造的な誤検出源**である。

→ 接続状態が変わったら検出器と診断の窓を捨てるようにした。

### 影検出: うなずきの誤検出が増加傾向

| 段階 | うなずき | 首振り |
|---|---|---|
| 6 | 0.43 回/分 | 1.9 回/分 |
| 7 | 0.6 回/分 | 1.6 回/分 |
| 8 | **1.1 回/分** | 1.5 回/分 |

うなずきが 3 回連続で増えている。応答までの 3 秒間に誤検出が起こる確率は約 2% → 5% に
上がった計算になる。誤ったうなずきは**意図しない帰路開始**という重い結果を生む。
`nod_pitch_threshold_deg` を ±8° から ±10° に戻すかどうかは判断待ち
(首振りと同じく「頻発したら対処」の方針なら現状維持)。

## 2026-08-18(段階 9)の結果

2 回分が混在(前回の消去忘れ)。新しい回は 15:31〜15:47。

### 迂回率 1.7 は実測どおりだった(以前の見立ては誤り)

再生ツールで確かめた結果、**経路長 235 m と速度積分 227 m がほぼ一致**していた。
GPS の水増しではなく、**実際に曲がりの多い経路を歩いていた**(直線 138 m に対し 235 m)。
精度不足で除外された fix は 0 件で、水平精度は 3〜5 m と良好だった。

段階 8 で「精度の悪い fix が迂回率を押し上げた」と判断したのは**この回には当てはまらない**。
段階 8 の回は確かに水平精度 69 m の区間があり、そこは水増しだったが、
**曲がりの多さそのものも実在した**ということになる。

| 回 | 直線 | 経路長 | 速度積分 | 迂回率 | 判定 |
|---|---|---|---|---|---|
| 段階 7 | 317 m | 419 m | 369 m | 1.32 | 一部水増し。真値は約 1.16 |
| 段階 9 | 138 m | 235 m | 227 m | **1.70** | 水増しなし。**実測どおり** |

**`detour_factor` の 1.3 は経路によって大きく外れる**(1.16〜1.70)。固定値で
帰宅時刻を約束するのは無理があり、動的化(docs/03)の必要性が実測で裏付けられた。
なお 1.3 は winding な経路では**楽観的すぎる**(帰りが想定より長くかかる)方向に外れる。

### 前後の聴き分けは不可能だった → 音の中身が原因

3D 化しても前後がほぼ判別できなかった。純音には前後判別に要る 4〜10 kHz の成分が
無いため。`noise_mix` で広帯域成分を混ぜる対応を入れた → docs/03

### 顔の向きの推定が逆側の定位を出した → 既定を false に戻した

推定が ±90° の上限に張り付き、自宅が正面にあるのに「右いっぱい」で鳴っていた → docs/03。
符号の反転か基準のドリフトかは未切り分けだが、**符号は屋内で確かめられる**。

### 影検出

107 回(うなずき 50 / 首振り 57)。ただしこの回は**意図的に首を振る指示**を含むため、
誤検出率としては前回までと比較できない。

## 2026-08-18 夜の結果と対処

31 分の散歩(途中ランニングあり)。音は最後まで鳴った。**音声エンジンの自己修復が動いた**
(10:00:02 に「再開に失敗(Session activation failed)」→ 10:00:07 に「再開しました」)。
前回の完全無音は解消している。

| 指摘 | 原因 | 対処 |
|---|---|---|
| 帰路イベントが発火せず手動発火した | プロンプトが `sessionEnd` 頼りで、帰路の時間を勘定していなかった | 予算条件で発火(docs/03)。同じログで **10:12 に発火**していた計算 |
| 方向音に「さっ」という雑音 | `noise_mix` が高すぎた(ビーコン 0.6) | ビーコン 0.2 / 提案音 0.15 へ |
| 左右の提案の半分は道が無いタイミング | 提案が探索範囲の縁(32〜35 m 手前)に集中。9 件中 7 件 | 曲がり角の誘導音(docs/03)。角に近づくほど間隔が詰まる |
| ランニングで速度が変わった | 予算模型の係数が固定(70 m/分)。実測は全体 94 m/分・帰路 76 m/分 | 未対処。係数の動的化(docs/03)が要る |

## 2026-08-18 夜・2 回目(d48245a の確認)の結果

同じ晩に 2 回目の散歩(12:37〜12:54。ログ消去忘れで前回の回と混在)。
体感の指摘と設計方針は docs/06 に集約した。ここには実測の裏付けだけを残す。

### 直ったことの確認

- **延長は歩ける時間になった**: 12:46:29「延長 +5 分(帰宅ぶん 6 分を別に確保)」→
  次の発火は 12:49:02(**2 分 33 秒後**・残り 9 分/帰宅推定 6 分)。修正前は 1 秒後に
  再発火し、4 回連続で首を振らせていた。利用者の体感も「正しく機能していそう」
- **歩き込んだ界隈でも分岐提案が鳴った**: 散策 11 分で 8 件、いずれも実在の分岐
  (交差点まで 13〜35 m)。比ゲートの狙いどおり。うち 2 件は score 0.80 と真に未踏の方向
- **誘導は「音のする方に歩けば角を曲がれる」ところまで来た**(利用者の体感)

### 新たに分かった欠陥(→ docs/06 柱 1)

- 間隔の変化は距離として伝わらない。**間隔固定+音量**に変える(利用者判断)
- 間隔が最小になるのは角まで 5 m 前後 = 曲がっている最中。ピークは角の**手前**に要る
- **曲がり終えると音が逆側へ流れる**。12:39:52〜58 に右へ曲がり終えた直後、
  鳴らす向きが +56° → −33° へ流れた。接近用のブレンド式が、通過後に
  「背後になった角」へ引き戻すため。構造の欠陥であり、パラメータでは直らない

### yaw の符号判定が出た(→ docs/06 柱 2)

35 対中 10 一致(29%)で**反転**。CoreMotion の規約(反時計回りが正)どおりの結論。
直進中の対が判定を薄めるので、|Δcourse| の大きい対だけ数える強化をしてから
全ログで確定させる。

### 迂回率の暴れが積み上がった(→ docs/06 柱 3)

この晩の 2 帰路は 1.61 と 1.09。段階 7 以降の実測は 1.09〜1.70 に散らばり、
固定係数 1.3 では帰宅推定がどちら側にも外れる。経路長ベースの推定に置き換える。

### 道具の不具合 3 点(→ docs/06 の段 0)

summarize が「提案(分岐)」を「鳴った」に数えない(10 件鳴ったのに 0 と要約)。
replay の却下内訳が旧ゲートの文字列のまま。replay が複数散歩の混在ログで
帰路をまとめて計算する(迂回率 4.63 という無意味な値)。

## 2026-08-19(散歩 α)の結果 — 顔基準は不採用

13 分の散歩 1 回(11:04〜11:17)。利用者の報告は 1 点、
**「案内音の左右が非常に分かりにくかった」**。

### 原因は顔基準。誘導音の設計ではなかった

自宅が真右にある区間で、進行基準の pan は +1.00 で安定していたのに、
顔基準は −0.97 → +0.04 → −0.01 と乱れていた(1 行目は真逆)。
ビーコンの繰り上げも「方向が 92° 変化」「90° 変化」が毎秒続いている。

**`use_head_orientation` を false に戻した。** 詳細と、符号ではなく
yaw そのものが旋回を追えていないことの実測は docs/03。

### 誘導音の設計は無罪だった(再生で確認・歩き直し不要)

「角を指すから正面寄りになり、左右が出ないのでは」という疑いは**再生で否定できた**。
記録した軌跡から誘導を組み直し、進行方位を基準にしたときの相対角を数えると:

| 相対角 | 音の数 | 割合 |
|---|---|---|
| ほぼ正面(<20°) | 39 | 8% |
| 斜め(20〜45°) | 88 | 17% |
| **横(≥45°)** | **376** | **75%** |

曲がり終えた回の典型は「接近中は正面寄り → 角で 70〜100° → 曲がった後は正面」。
狙いどおりの形になっている。**壊していたのは基準の側だけだった。**

### 終端は動作した

3 件中 2 件が「曲がり終えた」で終了(残り 1 件は最接近 1 m まで寄ってから離れた)。
以前は 8 件すべてが「角から離れた」で終わっていたので、終端の検出は効いている。
ただし**音量表現の体感は未評価**(左右が壊れた状態では判断できない)。

| 時刻 | 音 | 距離 | 終了 |
|---|---|---|---|
| 11:05:55 | 35 | 32m → 最接近 1m | 角から離れた |
| 11:08:35 | 26 | 30m → 最接近 2m | **曲がり終えた** |
| 11:09:30 | 29 | 34m → 最接近 2m | **曲がり終えた** |

### 判定ツールの誤りを直した

「yaw の符号」の節が**符号の一致率だけ**で判定しており、yaw が旋回と無関係でも
偏りがあれば「反転」と読めてしまう作りだった。これを真に受けて顔基準を有効にした。
`|Δraw|` の**大きさ**で判定するよう改めた(docs/03)。

## 2026-08-19(散歩 β)の結果 — 経路は当たり、確認音が案内をせき止めていた

7 分の散歩 1 回(12:47〜12:54)。利用者の報告は 2 点。

> 帰路の誘導音はかなり良いですが、帰宅開始時の一番向かう方向がわからない時に
> 音の頻度が少ない(中略)帰宅用の誘導音は散策時と別なのは音自体だけで、
> 鳴る間隔やタイミングは散策時の誘導と同じで良い

> 経路についてはあっていたと思います

### 経路は当たっていた

段 3・4 の主目的は達成。地図・経路・行き先はいずれも動いた。

- **経路の場**: 到達できる節点 57,056 / 57,486。構築 **0.2 秒**(実機・Debug)。
  背景で解く必要はなかったかもしれないが、待たされないという性質は保つ
- **帰宅推定**: 「残り 7 分・帰宅推定 4 分・**経路 267m**・73m/min」。
  経路長ベースで発火し、帰路は実際に 3.9 分だった
- **速度の実測化**: 「速度の推定を更新 なし → 66m/min(1 回目・設定値 70)」。
  次回からこの値が分母になる
- **誘導**: 7 件すべてが「曲がり終えた」または提案の停止で終了。逆側へ流れる例は無し

### 確認音が最初の 64 秒をせき止めていた

同意から最初の方向のある音まで **64 秒**。その間は方向の無い確認音が 8 回。
原因と対処は docs/03「returnAck の仕様」。

### 迂回率 1.44 — 固定係数では届かない例がまた 1 つ

| 回 | 直線 | 経路長 | 迂回率 |
|---|---|---|---|
| 段階 7 | 317m | 419m | 1.32 |
| 段階 9 | 138m | 235m | 1.70 |
| 08-18 夜 | 225m / 320m | 363m / 350m | 1.61 / 1.09 |
| **散歩 β** | 153m | 220m | **1.44** |

設定値 1.30 に対し実測は 1.09〜1.70。**経路長ベースへ移した判断を裏付けている**
(地図がある限りこの係数は使わない)。

### 気づいた点(未対処・報告のみ)

近接した節点で誘導が短く連続することがある(12:52:09 → 12:52:13 → 12:52:17 で
3 件、うち 2 件は 3 音ずつ)。カーブを短い線分で表した経路で、
`branch_straight_deg`(25°)を細かく跨ぐため。**利用者からの指摘は無い**ので、
困るまで手を入れない。

## 未検証の実装(実機で確かめる待ち行列)

実装済みで、まだ実機で確かめていないもの。ここが溜まったら散歩 1 回でまとめて見る。
**再生で確かめられるものはここに入れず、その場で片付ける。**

段階 9 で確認できたもの: 到着半径 10 m(鳴った)、ビーコン繰り上げ(動作)、
`fix` 行の記録(再生に足る粒度)、3D 音響(繋がったが前後は判別不能)。

**前後の聴き分けは HRTF では成立しないものとして扱う**(2026-08-18 の実測で確定)。
再テストは求めない。代わりに**音色で前後を分ける**実装を入れた(docs/03)。

| 実装 | 確かめ方 | 種別 |
|---|---|---|
| 真後ろの音色(`behind_darkness`) | ビーコンが背後方向のとき音が暗くなる | 実機・自動 |
| **帰路の出だし**(同意した直後から方向が分かるか) | 散歩 γ。確認音の短縮が効いているか | 実機・自動 |
| **ビーコンの歩調同期**(歩みに乗って聞こえるか) | 散歩 γ。要約の「歩調」欄で取得率も見る | 実機・自動 |
| **ビーコンの音量表現**(自宅の近さが分かるか) | 散歩 γ。距離を間隔から音量へ移した | 実機・自動 |
| **行き先の地帯が実際に働くか** | 散歩 γ。要約の「行き先」の回数と保った時間。β では 9 秒で死んでいた | 実機・自動 |

散歩 β までに消化: 経路の正しさ(当たっていた)、帰宅推定の経路長ベース化、
速度の実測化、誘導の終端、左右(顔基準を切って回復)、
**誘導の音量表現(音量で角を識別できると確認)**、
**「音の鳴る方へ歩くと面白い」ことの確認**(docs/01 の指針が実測で裏づけられた)。

**主観で聞くことは残っていない。** γ は普通に歩けばよく、
気づいたことがあれば教えてもらう形にする。行き先の地帯は β の時点で
9 秒しか動いていなかったので、修正後に働くかどうかはログで確かめる。

2026-08-18 夜の 2 回で消化: 経路データの読み込みと分岐提案(8 件、全て実在の分岐)、
yaw の符号(反転と判定)。

**意図的に試す項目は無くなった。** 次の実機テストの組み方は docs/06 の実験計画
(散歩 α / β)に従う。

### プロンプト保留は机上で検証済み(2026-08-18)

散歩に出ずに、デバッグの「時間到来を発火」→ AirPods を外す → 待つ → 付け直す、で確認できた。

```
08:18:07 時間になりました
08:18:08 ヘッドフォンが外れました
08:19:07 ヘッドフォン未装着のためプロンプトを保留します   ← 60 秒後の再プロンプトを抑制
08:20:07 ヘッドフォン未装着のためプロンプトを保留します   ← 120 秒後も抑制
08:20:42 ヘッドフォンを検出しました
08:20:42 再装着を検出したのでプロンプトを鳴らし直します    ← 即座に鳴らし直し
08:21:36 うなずきを検出 → 帰路へ
```

**154 秒外していても応答待ちは維持され、再装着で鳴り直した。** 仕様どおり。

段階 9 で「60 秒以上経つと反応しない」と見えていたのは、ログを確認すると
外していた最長が 19 秒で**再プロンプトの境界に達していなかった**ためだった
(外している間が無音なのは仕様であり、保留の経路は通っていなかった)。

### yaw の符号は再生ツールが自動で判定する

`HeadingFusion` は CoreMotion の yaw と方位の**回転の向きが揃っている**ことを前提にしている。
CoreMotion は反時計回りが正、方位は時計回りが正なので、揃っていなければ首を右に向けたときに
推定は左へ動く。

屋内で数字を読む代わりに、**歩行中の記録から自動で決める**ようにした。角を曲がれば
頭も体も同じ向きに回るので、Δyaw と Δcourse の符号が揃う対を数えればよい。
`scripts/replay_log.sh` が「yaw の符号」の節で判定を出す。**次の散歩のログで自動的に答えが出る**
(机上のテストでは曲がらないので判定できない)。

## 経路データを使うテストの手順(段階 10)

P2 の段取り 3〜5 を実装したので、その確認手順。**歩く前に 2 段階が入る。**

### 手順 1: 地図を作る(PC・数分)

```
brew install osmium-tool                      # 済んでいれば不要
# Geofabrik から地域の .osm.pbf を入手(例: Japan > Chubu / Kanto。数百 MB)
scripts/build_map.sh <地域pbf> <自宅の緯度> <自宅の経度>
```

出力は `maps/otosanpo-map.json`。節点数・道の本数・種別ごとの内訳が表示される。
**0 本なら中心座標か pbf の範囲が合っていない。**

### 手順 2: 記録済みのログで再生して確かめる(PC・歩かない)

```
scripts/replay_log.sh
```

過去の散歩の軌跡を新しい経路データに流し直す。見るところ:

- **「道に乗った」の割合**。低ければ `snap_max_distance_m` か地図の範囲を疑う
- **「道の向きと course が一致」の割合**。実地図なら高くなるはず
  (合成の格子地図で試した時は 25% だった。実地図での期待値はこれより大きく上がる)
- **「交差点に接近した回数」と「提案した回数」**。提案が 0 なら閾値が厳しすぎる。
  提案が交差点接近と同数なら間引きが効いていない
- 各提案の**相対角・道の種別・横断コスト**が妥当か。幹線ばかり選んでいれば
  `cross_cost_weight` / `way_class_weight` を上げる

**ここで納得できるまでパラメータを振る。** 値の調整に散歩は要らない。

### 手順 3: 地図を端末に入れて散歩する(実機・1 回)

1. Finder > iPhone > ファイル > OtoSanpo に `maps/otosanpo-map.json` をドラッグ
2. アプリを開き、画面に **`地図: 半径5km・<生成日>`** と出ることを確認
   (`未読込` なら転送先が違う。`(圏外)` なら地図の中心が自宅と離れている)
3. アプリで「ログを消去」→ 散歩開始
4. **意図的に試すのは 1 つだけ**: 時間到来の音の後、応答せず AirPods を外して
   **60 秒以上待ってから**付け直す(プロンプト保留の確認)
5. 自宅まで歩き切る
6. ログを `field-logs/inbox/` に入れる

観察したいこと(いずれも自動でログに残る):

- 提案が**曲がれる場所で**鳴るか。ログの「提案(分岐)」に交差点までの距離が出る
- 私有地・折り返しの提案が消えたか
- 幹線の横断を勧めてこないか
- ビーコンが背後方向のとき**音が暗くなる**のが分かるか
- 「地図: 圏外」に落ちる区間がないか

### 手順 4: 同じログをもう一度再生する

```
scripts/replay_log.sh
```

実機で鳴った提案と、再生での判断が一致するか。ずれていれば実機側の配線を疑う。

## 段階 9(次の実機テスト・1 回で上記すべて)

**先に机上で 3D を確認する。** AirPods を付けて ContentView の
「ビーコン(正面)」と「ビーコン(真後ろ)」を押し、聴き分けられるか。
同じに聞こえるならパンに落ちているので、そこを直してから散歩に出る
(イベントログに「3D」か「パン」のどちらで鳴ったか出る)。

そのうえで普通に散歩し、**意図的に試すのは 2 つだけ**。

1. 時間到来の音が鳴ったら、**応答せず AirPods を外し、60 秒以上待ってから付け直す**
   → プロンプト保留
2. 帰路の途中で、**歩きながら顔だけを左右に向ける**(体は進行方向のまま)
   → 頭の向きの反映。顔を右に向けたら音は左へ動くのが正しい

残りは普通に歩けばログに出る。自宅まで歩き切ること(迂回率は帰路でしか取れない)。

うまくいかない場合の逃げ道:

- 頭の向きがおかしい → `use_head_orientation` を false にすれば従来の挙動に戻る。
  ログには顔基準の pan と「進行基準なら何だったか」が併記されているので比較できる
- 到着音が鳴らない → `arrival_radius_m` を 15〜20 m へ広げる
